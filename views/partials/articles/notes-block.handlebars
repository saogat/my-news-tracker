<li>
    <p data-id = {{this.id}}>
	{{title}}
    </p>
    <a class="article-link" target="_blank" href={{link}}>{{title}}</a>
    <button class="note-button" data-id="{{this.id}}">Note</button>
</li>
